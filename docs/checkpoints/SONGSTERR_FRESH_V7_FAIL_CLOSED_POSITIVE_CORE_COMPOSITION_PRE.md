# SONGSTERR FRESH V7 FAIL-CLOSED POSITIVE-CORE COMPOSITION PRE

Status: **PROSPECTIVE / MECHANICAL COMPOSITION ONLY / FROZEN BEFORE IMPLEMENTATION OR OUTPUT**
Date: 2026-09-16 America/Toronto
Branch: `songsterr-fresh-pipeline-v1`
PRE parent head: `42e760e43a53ec0d17363b3905f64a8c0a5812a5`

## 1. PURPOSE AND NON-AUTHORITY

This PRE opens one synthetic-only mechanical integration line after the read-only theory review:

`docs/checkpoints/SONGSTERR_FRESH_V7_POST_KKT_FAIL_CLOSED_COMPOSITION_REVIEW.md`.

The line asks only whether the already-frozen independent evidence/protection roles can be composed deterministically under the prospectively justified **no-rescue / no-trade-off** rule.

It does not establish real correctness, calibrated precision, recall, model validation, customer eligibility, reattack repair, or Production readiness.

No real/media/model execution is authorized. No Basic Pitch, V6/V7 real correctness, EGFxSet, AG-PT/rejected holdout, protected song, physical capture/calibration, reserved Guitar Fretboard Notes data, GOAT/reference scoring, `songsterr_pipeline/**`, `main`, Production, or archived V143/Gomyway may be accessed or changed.

Global authority remains false/0/false exactly as recorded in `SONGSTERR_FRESH_PIPELINE_CURRENT_STATE.md`.

## 2. FROZEN COMPOSITION PRINCIPLE

For one selected proposal define:

- `S`: selected base support is available and valid under the frozen leakage-cleaned V3 support-template semantics;
- `E`: selected candidate evidence is available and `candidateEvidenceFraction >= 0.10` using the already-frozen V3 iteration-3 evidence-significance protection;
- `O`: lower-owner diagnostics are available and contain **zero** frozen veto owners;
- `K`: broad all-49 fixed-feature raw KKT strict necessity is available and certified.

The research positive-core condition is exactly:

`S AND E AND O AND K`.

No channel may rescue another channel. There is no weighted score, majority vote, rank, top-K, raw magnitude cutoff, learned parameter, candidate subset, per-MIDI exception, historical `0.01` transfer, or reattack fallback.

## 3. FROZEN TRI-STATE SEMANTICS

The new module may emit exactly these research composition states:

### `POSITIVE_CORE_CANDIDATE`

Use only when all are true:

- onset/raw evidence is available;
- `S=true`;
- `E=true`;
- `O=true`;
- `K=true`.

Meaning: all independently frozen conditions required by the positive research core are clear. This is an **unvalidated research state**, not a ground-truth or customer-admission claim.

### `PROTECTION_REJECTED`

Use only when:

- onset/raw evidence is available;
- selected base support is valid (`S=true`);
- required evidence/owner/KKT diagnostics are available;
- at least one of `E`, `O`, or `K` is false.

Persist deterministic rejection reasons drawn only from:

- `CANDIDATE_EVIDENCE_SIGNIFICANCE_FAILED` when `E=false`;
- `LOWER_OWNER_VETO_PRESENT` when `O=false`;
- `KKT_STRICT_RAW_NECESSITY_NOT_CERTIFIED` when `K=false`.

Multiple simultaneous reasons must all be retained in the fixed order above.

Meaning: the selected proposal is not corroborated by this positive-core contract. It does not claim ontological absence of the note.

### `UNRESOLVED_SUPPORT_OR_CONTEXT`

Use whenever any positive-core prerequisite cannot be validly evaluated, including:

- onset/raw evidence unavailable;
- selected base support invalid/ineligible (`S=false`);
- selected evidence/owner/KKT diagnostic unexpectedly unavailable or mechanically inconsistent.

This state is fail-closed. Raw/KKT evidence may be persisted diagnostically but cannot promote the proposal.

In particular, a KKT-certified support-ineligible reattack remains unresolved and receives no fallback.

## 4. HISTORICAL `0.01` IS FORBIDDEN IN COMPOSITION

The module may preserve historical raw/support values only inside frozen dependency payloads for lineage. It MUST NOT:

- compare any broad raw necessity fraction to `0.01`;
- compare any support-space necessity fraction to `0.01` as a positive-core gate;
- import `NECESSITY_FRACTION_MIN` as a composition threshold;
- emit a field suggesting historical necessity threshold was applied.

The only retained numeric protection in composition is the already-frozen candidate-evidence significance minimum `0.10` in its original support semantics.

KKT contributes only its existing Boolean strict-necessity certificate.

## 5. FROZEN DEPENDENCY SOURCE

The composition module must obtain its measurements from the already-frozen KKT diagnostic packet:

`scripts/songsterr-fresh/v7_kkt_raw_necessity_diagnostics_v1.py`
blob `2daa9f7f6983a3ec894fc08a86e9bced7b1f96c4`.

That frozen packet already binds:

- raw V6 onset innovation;
- all-49 gate-free fixed-feature raw problem;
- KKT strict-necessity certificate;
- selected base support validity;
- selected candidate-evidence diagnostics;
- selected lower-owner diagnostics.

The new composition module must not recompute alternative support, owner, evidence, candidate-population, feature-universe or KKT semantics.

It may only validate the frozen payload and compose its existing fields.

## 6. PROSPECTIVE PURE BOOLEAN TRUTH-TABLE GATE

Before any audio-fixture output is observed, the test harness must freeze a pure composition helper and exhaust all `2^4 = 16` Boolean combinations of `S,E,O,K` under otherwise available diagnostics.

Expected state is frozen prospectively:

- if `S=false`: `UNRESOLVED_SUPPORT_OR_CONTEXT` regardless of `E,O,K`;
- if `S=true` and `E=true` and `O=true` and `K=true`: `POSITIVE_CORE_CANDIDATE`;
- if `S=true` and any of `E,O,K` is false: `PROTECTION_REJECTED`.

For `PROTECTION_REJECTED`, expected reasons are exactly the failed subset of `E,O,K` in the fixed order from section 3.

The truth-table gate must run three times in-process and canonical outputs must be byte-identical.

This truth table is the authoritative mechanical acceptance gate. It does not use any audio-fixture expected classification.

## 7. EXACT 23-FIXTURE MECHANICAL PASS

The existing frozen V6 23-fixture manifest may be used only to verify integration mechanics and deterministic routing through the already-frozen dependency measurements.

The test MUST NOT use each fixture's historical `expectedClassification` to determine or judge the new composition state.

For every fixture:

1. generate the exact frozen deterministic synthetic waveform;
2. call the new composition module exactly once per repetition;
3. independently call the frozen KKT diagnostic and require the embedded/final dependency payload to match canonically;
4. recompute the expected composition state **only from the dependency booleans under the truth-table rule**, never from fixture labels;
5. require exact state/reason equality;
6. require no rescue when support is invalid;
7. require no historical `0.01`, rank/top-K, raw magnitude cutoff, score, or fallback field;
8. preserve the selected MIDI exactly.

The 23-fixture pass must run three times in-process and be byte-identical.

No class-count expectation is frozen by this PRE. Any observed distribution is descriptive only and may not be used to revise the rule after output.

## 8. UNTOUCHED V3 REGRESSION

The one-shot workflow must also execute the untouched frozen V3 iteration-3 34-fixture regression exactly once.

Expected unchanged result remains 34 fixtures ×3 deterministic PASS with mismatch `0`.

This is a dependency-regression guard only, not evidence that the new composition is externally correct.

## 9. REQUIRED OUTPUT CONTRACT

The composition module must persist at minimum:

- contract/version;
- selected MIDI;
- `compositionState`;
- ordered `protectionRejectionReasons`;
- `positiveCoreDefined:true`;
- `customerDecisionDefined:false`;
- `realCorrectnessDefined:false`;
- `historicalNecessityThresholdApplied:false`;
- `rawMagnitudeThresholdDefined:false`;
- `rankCutoffDefined:false`;
- `scoreDefined:false`;
- `reattackFallbackDefined:false`;
- `supportEligible` when evaluable;
- `candidateEvidencePass` when evaluable;
- `noLowerOwnerVeto` when evaluable;
- `kktStrictRawNecessityCertified` when evaluable;
- the complete frozen KKT diagnostic payload used as composition input.

A positive-core state must never exist when any of `S,E,O,K` is false or unavailable.

## 10. FROZEN WRITE BOUNDARY

After this PRE commit exists, this attempt may create/change only:

- `scripts/songsterr-fresh/v7_fail_closed_positive_core_v1.py` — new mechanical composition module;
- `scripts/songsterr-fresh/test_v7_fail_closed_positive_core_v1.py` — new truth-table + 23-fixture mechanical harness;
- `.github/workflows/songsterr-v7-fail-closed-positive-core-one-shot.yml` — self-scoped attempt-1 workflow;
- `docs/checkpoints/SONGSTERR_FRESH_V7_FAIL_CLOSED_POSITIVE_CORE_COMPOSITION_RESULT.md` — immutable attempt-1 result;
- `docs/checkpoints/SONGSTERR_FRESH_PIPELINE_CURRENT_STATE.md` — state transitions only.

No other path is authorized by this PRE.

## 11. ONE-SHOT EXECUTION CONTRACT

Before first output:

1. create module and test only;
2. commit first versions;
3. compare this PRE commit to pair head and require exactly those two added Python files;
4. pin exact pair/dependency Git blobs;
5. update `CURRENT_STATE` with the unexecuted frozen pair;
6. create only the self-scoped workflow;
7. use Python `3.10.21`, NumPy `1.26.4`, SciPy `1.15.3`;
8. static guard must reject network/model/media/subprocess/repository-mutation paths;
9. run the new composition harness exactly once plus untouched V3 regression exactly once;
10. upload artifact `songsterr-fresh-v7-fail-closed-positive-core` containing `fail-closed-positive-core.json` and `v3-regression.txt` even on failure;
11. attempt 1 is authoritative; no rescue rerun or post-output rule adjustment.

If a mechanical implementation defect appears, freeze it. Any revised executable attempt requires a new prospective PRE before code changes.

## 12. INTERPRETATION BOUNDARY

A successful mechanical attempt can establish only that the no-rescue positive-core rule is implemented exactly and deterministically over frozen measurements.

It cannot establish:

- external precision or recall;
- real-guitar correctness;
- model validation;
- customer eligibility;
- that support-ineligible reattacks are false;
- that unresolved cases may be rescued;
- permission to run real/media/model evaluation.

Any future external evaluation requires its own prospective real-evaluation/scoring PRE plus fresh explicit user authorization.

Archived V143/Gomyway remains untouched.