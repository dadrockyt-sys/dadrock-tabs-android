name: V157 generate reference-blind CPU candidate once

on:
  push:
    branches:
      - v143-contextual-prune-lobo
    paths:
      - .github/workflows/v157-generate-reference-blind-once.yml

permissions:
  contents: write

env:
  BRANCH: v143-contextual-prune-lobo
  CUDA_VISIBLE_DEVICES: ""
  OMP_NUM_THREADS: "1"
  MKL_NUM_THREADS: "1"
  OPENBLAS_NUM_THREADS: "1"
  NUMEXPR_NUM_THREADS: "1"
  PYTHONHASHSEED: "0"
  CUBLAS_WORKSPACE_CONFIG: ":4096:8"
  SOURCE_DIR: /tmp/v157-historical-source
  NORM_WAV: /tmp/v157-normalized.wav
  DEMUCS_DIR: /tmp/v157-demucs
  PREREG: debug/v157-cpu-autonomous/preregistration.json
  PRE_RUN: debug/v157-cpu-autonomous/pre-run-receipt.json
  TRANSCRIBER: validation/v157_cpu_multitrack/transcribe_hybrid.py
  QC: validation/v157_cpu_multitrack/structural_qc.py
  ENGINE: validation/v155_cpu_multitrack/transcribe_hybrid.py
  ENV_RECEIPT: debug/v157-cpu-autonomous/environment-receipt.json
  OUTPUT: debug/v157-cpu-autonomous/generated.json
  RECEIPT: debug/v157-cpu-autonomous/generation-receipt.json
  QC_OUTPUT: debug/v157-cpu-autonomous/structural-qc.json

jobs:
  generate-once:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v143-contextual-prune-lobo
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Verify sole-trigger and sealed identities
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_RUN_NUMBER" = "1"
          test "$(git hash-object "$PREREG")" = "0bcca0fbb53abcc8ad8736d8e5e71e32a14004f6"
          test "$(git hash-object "$PRE_RUN")" = "86e3419203f7001d2431f4bcf62113cbaba67786"
          test "$(git hash-object "$TRANSCRIBER")" = "1d1725e2d79b173bd5fb0bfa7aefc25dce81dd58"
          test "$(git hash-object "$QC")" = "5ff4df5b5c6b700272b349a1bbe709b15e17e794"
          test "$(git hash-object "$ENGINE")" = "3357582dd8311b28f4b85f2ebfbc7acb8c9e4fb8"
          test ! -e "$OUTPUT"
          test ! -e "$RECEIPT"
          test ! -e "$QC_OUTPUT"
          test ! -e "$ENV_RECEIPT"
          python -m py_compile "$TRANSCRIBER" "$QC" "$ENGINE"
          if grep -En 'research/v154-professional-references|frontend-reference-payload|score_frontend_reference|debug/v154-cpu-autonomous/broad-other-run|debug/v155-cpu-autonomous/generated|debug/v156-cpu-autonomous/generated' "$TRANSCRIBER" "$QC" "$ENGINE"; then
            echo 'Forbidden scored/reference/generated-output path dependency in V157 generation code' >&2
            exit 1
          fi
          python - <<'PY'
          import json
          from pathlib import Path
          p = json.loads(Path('debug/v157-cpu-autonomous/preregistration.json').read_text())
          r = json.loads(Path('debug/v157-cpu-autonomous/pre-run-receipt.json').read_text())
          assert p['version'] == 'V157'
          assert p['status'] == 'PREREGISTERED_BEFORE_GENERATION'
          assert p['execution']['device'] == 'CPU_ONLY'
          assert p['execution']['singleCandidate'] is True
          assert p['execution']['singleGenerationRun'] is True
          assert p['executionDeterminism']['rngSeed'] == 0
          assert p['executionDeterminism']['demucsInvocation'].startswith('in-process')
          assert p['triggerSafety']['generationWorkflowCreationIsTheSingleTrigger'] is True
          assert p['triggerSafety']['secondArmEditForbidden'] is True
          assert p['modelIdentityReceiptRepair']['importOsRequired'] is True
          assert p['modelIdentityReceiptRepair']['noSecondSeparationForReceipt'] is True
          assert r['validation'] == 'PASS'
          assert r['candidateExistsAtSeal'] is False
          assert r['referenceFacingScoreCallsAtSeal'] == 0
          assert r['triggerSafety']['workflowCreationIsSingleTrigger'] is True
          assert r['triggerSafety']['secondArmEditForbidden'] is True
          PY

      - name: Materialize exact historical audio
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$SOURCE_DIR"
          git show 74b0f815ff3f66f325220975c410621503de440f:public/gomywayfullaitest.m4a > "$SOURCE_DIR/gomywayfullaitest.m4a"
          test "$(sha256sum "$SOURCE_DIR/gomywayfullaitest.m4a" | awk '{print $1}')" = "215bd5a657c5326f08f132ae358595a95c30b39bb7493a52c2f910d5a608149f"
          test "$(wc -c < "$SOURCE_DIR/gomywayfullaitest.m4a" | tr -d ' ')" = "3478611"

      - name: Install exact CPU dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install --disable-pip-version-check numpy==1.26.4 scipy==1.13.1 soundfile==0.12.1 basic-pitch==0.4.0 demucs==4.1.0 imageio-ffmpeg==0.6.0 librosa==0.11.0
          python - <<'PY'
          import importlib.metadata, numpy, scipy, soundfile, torch
          assert numpy.__version__ == '1.26.4'
          assert scipy.__version__ == '1.13.1'
          assert soundfile.__version__ == '0.12.1'
          assert importlib.metadata.version('basic-pitch') == '0.4.0'
          assert importlib.metadata.version('demucs') == '4.1.0'
          assert importlib.metadata.version('imageio-ffmpeg') == '0.6.0'
          assert importlib.metadata.version('librosa') == '0.11.0'
          assert torch.__version__ == '2.8.0+cpu'
          assert torch.version.cuda is None
          assert torch.cuda.is_available() is False
          PY
          PYTHONPATH="$GITHUB_WORKSPACE" python -m validation.v157_cpu_multitrack.transcribe_hybrid --help >/dev/null
          PYTHONPATH="$GITHUB_WORKSPACE" python -m validation.v157_cpu_multitrack.structural_qc --help >/dev/null

      - name: Deterministically normalize audio
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib, subprocess
          from pathlib import Path
          import imageio_ffmpeg
          src = Path('/tmp/v157-historical-source/gomywayfullaitest.m4a')
          out = Path('/tmp/v157-normalized.wav')
          exe = imageio_ffmpeg.get_ffmpeg_exe()
          subprocess.check_call([exe, '-v', 'error', '-i', str(src), '-vn', '-ar', '44100', '-ac', '2', '-c:a', 'pcm_s16le', str(out)])
          assert out.is_file() and out.stat().st_size > 1000000
          digest = hashlib.sha256(out.read_bytes()).hexdigest()
          assert digest == '3e61b7926eabc21b758c750f826c7426a29d6de5aafdd5c93f8045ecdc67f87e'
          print('normalized_sha256=' + digest)
          PY

      - name: Seeded in-process CPU Demucs separation
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import random
          from pathlib import Path
          import numpy as np
          import torch

          seed = 0
          random.seed(seed)
          np.random.seed(seed)
          torch.manual_seed(seed)
          torch.set_num_threads(1)
          torch.set_num_interop_threads(1)
          torch.use_deterministic_algorithms(True, warn_only=False)
          assert torch.version.cuda is None
          assert torch.cuda.is_available() is False
          assert torch.get_num_threads() == 1
          assert torch.get_num_interop_threads() == 1

          from demucs.separate import main as demucs_main
          args = ['-n', 'htdemucs_6s', '-d', 'cpu', '--shifts', '1', '-j', '1', '-o', '/tmp/v157-demucs', '/tmp/v157-normalized.wav']
          try:
              demucs_main(args)
          except SystemExit as exc:
              if exc.code not in (None, 0):
                  raise
          stem_dir = Path('/tmp/v157-demucs/htdemucs_6s/v157-normalized')
          for name in ('guitar.wav', 'bass.wav', 'drums.wav'):
              p = stem_dir / name
              assert p.is_file() and p.stat().st_size > 1000000, p
          print('stem_dir=' + str(stem_dir))
          PY

      - name: Refresh branch before candidate boundary
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin "$BRANCH"
          git rebase "origin/$BRANCH"
          test "$GITHUB_RUN_NUMBER" = "1"
          test "$(git hash-object "$PREREG")" = "0bcca0fbb53abcc8ad8736d8e5e71e32a14004f6"
          test "$(git hash-object "$PRE_RUN")" = "86e3419203f7001d2431f4bcf62113cbaba67786"
          test "$(git hash-object "$TRANSCRIBER")" = "1d1725e2d79b173bd5fb0bfa7aefc25dce81dd58"
          test "$(git hash-object "$QC")" = "5ff4df5b5c6b700272b349a1bbe709b15e17e794"
          test "$(git hash-object "$ENGINE")" = "3357582dd8311b28f4b85f2ebfbc7acb8c9e4fb8"
          test ! -e "$OUTPUT"
          test ! -e "$RECEIPT"
          test ! -e "$QC_OUTPUT"
          test ! -e "$ENV_RECEIPT"

      - name: Freeze environment and separation receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p debug/v157-cpu-autonomous
          python - <<'PY'
          import hashlib, importlib.metadata, json, os
          from pathlib import Path
          import numpy, scipy, soundfile, torch

          def sha256(path):
              h = hashlib.sha256()
              with Path(path).open('rb') as f:
                  for chunk in iter(lambda: f.read(1024 * 1024), b''):
                      h.update(chunk)
              return h.hexdigest()

          stem_dir = Path('/tmp/v157-demucs/htdemucs_6s/v157-normalized')
          roots = [
              Path.home() / '.cache' / 'torch' / 'hub' / 'checkpoints',
              Path.home() / '.cache' / 'huggingface' / 'hub',
          ]
          by_resolved = {}
          for root in roots:
              if not root.exists():
                  continue
              for logical in sorted(root.rglob('*')):
                  try:
                      if not logical.is_file():
                          continue
                      low_parts = [p.lower() for p in logical.parts]
                      name = logical.name.lower()
                      if '.locks' in low_parts or name.endswith('.lock') or name.endswith('.json') or name.endswith('.txt') or name in {'refs', 'readme', 'readme.md'}:
                          continue
                      resolved = logical.resolve(strict=True)
                      if not resolved.is_file():
                          continue
                      size = resolved.stat().st_size
                      if size < 1048576:
                          continue
                      key = str(resolved)
                      by_resolved.setdefault(key, {
                          'logicalPath': str(logical),
                          'resolvedPath': key,
                          'bytes': size,
                          'sha256': sha256(resolved),
                      })
                  except (FileNotFoundError, OSError):
                      continue
          model_blobs = [by_resolved[k] for k in sorted(by_resolved)]
          assert model_blobs, 'No resolved >=1MiB Demucs model blob found in preregistered cache roots'

          receipt = {
              'schema': 'dadrock.tabs.v157.cpu-environment-receipt.v1',
              'validation': 'PASS',
              'workflowRunId': os.environ.get('GITHUB_RUN_ID'),
              'workflowRunNumber': int(os.environ.get('GITHUB_RUN_NUMBER', '0')),
              'device': 'cpu',
              'cudaAvailable': False,
              'torchCudaVersion': None,
              'versions': {
                  'python': '3.10',
                  'torch': torch.__version__,
                  'numpy': numpy.__version__,
                  'scipy': scipy.__version__,
                  'soundfile': soundfile.__version__,
                  'demucs': importlib.metadata.version('demucs'),
                  'basic-pitch': importlib.metadata.version('basic-pitch'),
                  'librosa': importlib.metadata.version('librosa'),
                  'imageio-ffmpeg': importlib.metadata.version('imageio-ffmpeg'),
              },
              'v157EngineGitBlob': '3357582dd8311b28f4b85f2ebfbc7acb8c9e4fb8',
              'demucsModel': 'htdemucs_6s',
              'demucsShifts': 1,
              'demucsJobs': 1,
              'demucsModelBlobFiles': model_blobs,
              'modelCacheRootsScanned': [str(p) for p in roots],
              'determinism': {
                  'seed': 0,
                  'pythonRandomSeed': 0,
                  'numpyRandomSeed': 0,
                  'torchManualSeed': 0,
                  'torchDeterministicAlgorithms': True,
                  'torchDeterministicWarnOnly': False,
                  'torchNumThreads': 1,
                  'torchNumInteropThreads': 1,
                  'ompNumThreads': 1,
                  'mklNumThreads': 1,
                  'openblasNumThreads': 1,
                  'numexprNumThreads': 1,
                  'demucsInvocation': 'in-process demucs.separate.main',
                  'separationRepeatCount': 1,
              },
              'audio': {
                  'sourceSha256': '215bd5a657c5326f08f132ae358595a95c30b39bb7493a52c2f910d5a608149f',
                  'normalizedSha256': sha256('/tmp/v157-normalized.wav'),
              },
              'stemSha256': {
                  'guitar': sha256(stem_dir / 'guitar.wav'),
                  'bass': sha256(stem_dir / 'bass.wav'),
                  'drums': sha256(stem_dir / 'drums.wav'),
              },
              'safety': {
                  'referenceRead': False,
                  'professionalReferencePathsOpened': 0,
                  'referenceFacingScoreCalls': 0,
                  'gpuUsed': False,
                  'modalUsed': False,
                  'mainOrProductionModified': False,
              },
          }
          assert receipt['workflowRunNumber'] == 1
          Path('debug/v157-cpu-autonomous/environment-receipt.json').write_text(json.dumps(receipt, indent=2, sort_keys=True) + '\n')
          print(json.dumps({'modelBlobCount': len(model_blobs), 'stemSha256': receipt['stemSha256']}, indent=2, sort_keys=True))
          PY

      - name: Generate exactly one V157 candidate
        shell: bash
        run: |
          set -euo pipefail
          STEM_DIR="$DEMUCS_DIR/htdemucs_6s/v157-normalized"
          PYTHONPATH="$GITHUB_WORKSPACE" python -m validation.v157_cpu_multitrack.transcribe_hybrid \
            --mix "$NORM_WAV" \
            --guitar "$STEM_DIR/guitar.wav" \
            --bass "$STEM_DIR/bass.wav" \
            --drums "$STEM_DIR/drums.wav" \
            --preregistration "$PREREG" \
            --environment-receipt "$ENV_RECEIPT" \
            --output "$OUTPUT" \
            --receipt "$RECEIPT"
          test -f "$OUTPUT"
          test -f "$RECEIPT"

      - name: Independent reference-blind structural QC
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH="$GITHUB_WORKSPACE" python -m validation.v157_cpu_multitrack.structural_qc \
            --candidate "$OUTPUT" \
            --receipt "$RECEIPT" \
            --preregistration "$PREREG" \
            --pre-run-receipt "$PRE_RUN" \
            --output "$QC_OUTPUT"
          python - <<'PY'
          import json
          from pathlib import Path
          q = json.loads(Path('debug/v157-cpu-autonomous/structural-qc.json').read_text())
          assert q['validation'] == 'PASS'
          assert q['safety']['referenceRead'] is False
          assert q['safety']['referenceFacingScoreCalls'] == 0
          PY

      - name: Freeze candidate receipts and self-seal
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/v157-generate-reference-blind-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$OUTPUT" "$RECEIPT" "$QC_OUTPUT" "$ENV_RECEIPT" .github/workflows/v157-generate-reference-blind-once.yml
          git commit -m "research: freeze sole V157 reference-blind CPU candidate [skip ci]"
          git push origin HEAD:"$BRANCH"
