name: V143 Async Nested Worker Smoke

on:
  push:
    branches:
      - v143-contextual-prune-lobo
    paths:
      - analyzer/v143_async_nested_worker_smoke_modal.py
      - .github/workflows/v143-async-nested-worker-smoke.yml

concurrency:
  group: v143-async-nested-worker-smoke
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  async-nested-worker-smoke:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      MODAL_ENVIRONMENT: main
      PYTHONUNBUFFERED: "1"
      MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
      MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
      V143_ASYNC_NESTED_SMOKE_APP_NAME: dadrock-v143-async-nested-worker-smoke
      V143_ASYNC_NESTED_SMOKE_QUEUE_NAME: dadrock-v143-async-nested-worker-smoke-results
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Modal client
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip >/dev/null
          python -m pip install 'modal==1.5.5' >/dev/null
          modal token set --token-id "$MODAL_TOKEN_ID" --token-secret "$MODAL_TOKEN_SECRET" >/dev/null
          modal --version

      - name: Deploy isolated nested-worker smoke app
        shell: bash
        run: |
          set -euo pipefail
          modal deploy --env main analyzer/v143_async_nested_worker_smoke_modal.py

      - name: Invoke exactly one spawned nested worker dependency smoke
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p debug/v143-contextual-prune/async-nested-worker-smoke
          timeout 420s python - <<'PY'
          import json
          from pathlib import Path
          import modal

          safe = {
              'schemaVersion': 1,
              'gate': 'v143-async-nested-worker-smoke',
              'audioRead': False,
              'separatorModelExecuted': False,
              'referenceFacingInputs': 0,
              'referenceScoreCalls': 0,
              'qualityVerdictMade': False,
          }
          try:
              fn = modal.Function.from_name(
                  'dadrock-v143-async-nested-worker-smoke',
                  'nested_worker_spawn_smoke',
                  environment_name='main',
              )
              result = fn.remote()
              safe.update(result)
              safe['status'] = 'success'
          except BaseException as error:
              safe.update({
                  'status': 'failed',
                  'errorClass': type(error).__name__,
                  'errorMessage': str(error)[:500],
              })
          Path('debug/v143-contextual-prune/async-nested-worker-smoke/summary.json').write_text(
              json.dumps(safe, indent=2, sort_keys=True) + '\n', encoding='utf-8'
          )
          print(json.dumps(safe, indent=2, sort_keys=True))
          if safe.get('status') != 'success':
              raise SystemExit(1)
          if safe.get('nestedWorkerCompleted') is not True:
              raise SystemExit(1)
          if safe.get('queueCleared') is not True:
              raise SystemExit(1)
          if safe.get('resultTtlSeconds') != 900:
              raise SystemExit(1)
          if safe.get('workerGpu') != 'NVIDIA L4':
              raise SystemExit(1)
          PY

      - name: Capture isolated app logs
        if: always()
        shell: bash
        run: |
          set +e
          echo '=== isolated nested-worker logs ==='
          modal app logs "$V143_ASYNC_NESTED_SMOKE_APP_NAME" --env main --since 1h --tail 1500 --timestamps --show-function-id --show-function-call-id --show-container-id 2>&1
          exit 0

      - name: Preserve aggregate evidence only
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v143-async-nested-worker-smoke
          path: debug/v143-contextual-prune/async-nested-worker-smoke/summary.json
          if-no-files-found: warn
          retention-days: 7

      - name: Stop isolated app
        if: always()
        shell: bash
        run: |
          set +e
          modal app stop "$V143_ASYNC_NESTED_SMOKE_APP_NAME" --env main --yes
          exit 0
