name: Clean up tab preview card

# TEMPORARY research-branch read-only validator; restore after this one run.
on:
  push:
    paths:
      - '.github/workflows/cleanup-tab-preview.yml'

permissions:
  contents: read

jobs:
  patch-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v143-contextual-prune-lobo
          fetch-depth: 1
      - name: Validate V154 Lead timing draft alignment
        run: python validation/v154_cpu_multitrack/validate_lead_timing_draft.py
