name: Checkpoint V168 shared-context gate

on:
  push:
    branches: [v143-contextual-prune-lobo]
    paths:
      - .github/workflows/v168-shared-context-gate-checkpoint.yml

permissions:
  contents: write

jobs:
  checkpoint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v143-contextual-prune-lobo
          fetch-depth: 0
      - name: Append frozen pre-execution gate and self-delete
        shell: bash
        run: |
          set -euo pipefail
          cat >> docs/checkpoints/CURRENT_STATE.md <<'EOF'

## 2026-09-01 continuation checkpoint — shared-context functional gate frozen before execution
- Exact V166 Actions run `33226705813` was checked for surviving workflow artifacts: **none exist**. Historical `.github/workflows/v166-generate.yml` at head `1e06d775ee03ebf92fe8f68fc02cf034812ea43f` contains no `upload-artifact` step and did not persist Demucs stems/cache; its terminal commit was restricted to timebase/QC/candidate/receipts. Therefore the authoritative historical Drums WAV bytes cannot currently be recovered from the V166 run.
- Frozen V166 generation receipt remains authoritative: candidate SHA256 **`fa2411598b401f745eff49a9cbda294ed767de093c905909531c7dd4dc6eb378`**, timebase SHA256 **`899746d3048d239bc0032375d412a109ea04b055df19df1b7b08dc3e73aa5ca0`**, normalized mix SHA256 **`3e61b7926eabc21b758c750f826c7426a29d6de5aafdd5c93f8045ecdc67f87e`**, 1050 combined-Guitar events and 402 Bass events.
- Added isolated no-Basic-Pitch checker `validation/v168_splitmysong_diagnostic/check_shared_context_equivalence_v168.py`, creation commit **`73af04264c13e5daddb5b7fc1db4e0bd9dcea149`**, frozen Git blob **`2e9a8dc2ec60a311b3e6bca5f8dc9390aea48b0f`**.
- The checker reproduces the exact frozen shared-onset calculation (`0.65 * unit(drums onset) + 0.35 * unit(mix onset)`) and exact V166/V165/V164 event-step logic. For every persisted V166 `stepSelection` candidate row it recomputes the current shared support from a drifted Drums stem while holding the event's historical instrument support fixed, recomputes the exact event-step score, and reselects the winner under the frozen non-nearest-margin/tie rule.
- **Acceptance was frozen before execution and is intentionally fail-closed:** PASS requires (1) all 1805 frozen lattice steps represented by persisted historical option rows, (2) zero inconsistent repeated historical shared-support values, (3) zero stored historical score recomputation mismatches, (4) zero frozen winner recomputation mismatches, and (5) zero current selected-step changes. Incomplete lattice coverage is `FAIL_CLOSED` even if every observed historical event decision agrees.
- The diagnostic will report continuous support/score drift and lattice coverage but will never claim WAV-byte equivalence. It accepts no professional-reference/scorer path and no SplitMySong Guitar input; it imports/invokes no Basic Pitch and generates no candidate.
- No one-shot SplitMySong Basic Pitch attempt has been consumed. No scorer/reference access or reference-facing call occurred. **Project Progress Score remains 60%; Test Score remains NOT RUN; SplitMySong Basic Pitch attempts remain 0; V168 reference-facing score calls remain 0.**
EOF
          rm .github/workflows/v168-shared-context-gate-checkpoint.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/checkpoints/CURRENT_STATE.md .github/workflows/v168-shared-context-gate-checkpoint.yml
          git commit -m "Checkpoint shared-context gate before execution"
          git push origin HEAD:v143-contextual-prune-lobo
