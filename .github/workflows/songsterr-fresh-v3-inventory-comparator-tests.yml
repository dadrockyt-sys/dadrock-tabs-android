name: Songsterr Fresh V3 Duration Diagnostic Tests

on:
  workflow_dispatch:
  push:
    branches:
      - songsterr-fresh-pipeline-v1
    paths:
      - '.github/workflows/songsterr-fresh-v3-inventory-comparator-tests.yml'
      - 'scripts/songsterr-fresh/compare_v3_unresolved_duration_inventories.py'
      - 'scripts/songsterr-fresh/summarize_v3_unresolved_duration_evidence.py'
      - 'scripts/songsterr-fresh/summarize_v3_reattack_structure_context.py'
      - 'scripts/songsterr-fresh/summarize_v3_repeated_note_structure_context.py'
      - 'scripts/songsterr-fresh/probe_v3_activation_spectral_rejection_context.py'
      - 'scripts/songsterr-fresh/probe_v3_spectral_valley_neighborhood_context.py'
      - 'scripts/songsterr-fresh/probe_v3_post_valley_spectral_trajectory_context.py'

permissions:
  contents: read

jobs:
  diagnostic-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out fresh branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Compile descriptive duration tools
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile \
            scripts/songsterr-fresh/summarize_v3_unresolved_duration_evidence.py \
            scripts/songsterr-fresh/compare_v3_unresolved_duration_inventories.py \
            scripts/songsterr-fresh/summarize_v3_reattack_structure_context.py \
            scripts/songsterr-fresh/summarize_v3_repeated_note_structure_context.py \
            scripts/songsterr-fresh/probe_v3_activation_spectral_rejection_context.py \
            scripts/songsterr-fresh/probe_v3_spectral_valley_neighborhood_context.py \
            scripts/songsterr-fresh/probe_v3_post_valley_spectral_trajectory_context.py

      - name: Prove inventory comparator remains fail-closed and deterministic
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/compare_v3_unresolved_duration_inventories.py --self-test

      - name: Prove structure-context diagnostic remains read-only and deterministic
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/summarize_v3_reattack_structure_context.py --self-test

      - name: Prove repeated-note diagnostic remains read-only and deterministic
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/summarize_v3_repeated_note_structure_context.py --self-test

      - name: Prove spectral-rejection context probe contract stays read-only
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/probe_v3_activation_spectral_rejection_context.py --self-test

      - name: Prove spectral-valley neighborhood probe stays read-only
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/probe_v3_spectral_valley_neighborhood_context.py --self-test

      - name: Prove post-valley spectral trajectory probe stays read-only
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/probe_v3_post_valley_spectral_trajectory_context.py --self-test
