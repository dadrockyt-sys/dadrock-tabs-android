name: Songsterr Fresh Gomyway Midterm Note Evidence Canary

on:
  workflow_dispatch:
  push:
    branches:
      - songsterr-fresh-pipeline-v1
    paths:
      - '.github/workflows/songsterr-fresh-gomyway-midterm-note-evidence-canary.yml'
      - 'scripts/songsterr-fresh/analyze_structure_conditioned_notes.py'
      - 'scripts/songsterr-fresh/inspect_role_boundary_evidence.py'
      - 'scripts/songsterr-fresh/estimate_selected_pitch_releases.py'
      - 'scripts/songsterr-fresh/build_note_evidence_context.mjs'
      - 'scripts/songsterr-fresh/build_note_evidence.mjs'
      - 'scripts/songsterr-fresh/run_note_evidence_pipeline_canary.mjs'
      - 'songsterr_pipeline/noteEventExposure.mjs'
      - 'songsterr_pipeline/noteEvidenceAdapter.mjs'
      - 'songsterr_pipeline/noteEvidenceDiagnostics.mjs'
      - 'songsterr_pipeline/noteEvidenceEvaluator.mjs'
      - 'songsterr_pipeline/structureIdentity.mjs'
      - 'songsterr_pipeline/tests/noteEventExposure.test.mjs'
      - 'songsterr_pipeline/tests/noteEvidenceAdapter.test.mjs'
      - 'songsterr_pipeline/tests/noteEvidenceDiagnostics.test.mjs'
      - 'songsterr_pipeline/tests/noteEvidenceEvaluator.test.mjs'

permissions:
  contents: read

jobs:
  note-evidence-canary:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out fresh branch
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch exact authorized audio fixture
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location --silent --show-error --retry 3 \
            'https://raw.githubusercontent.com/dadrockyt-sys/dadrock-tabs-android/main/public/jimmy-paige-midterm-v1/gomyway-midterm-source.m4a' \
            --output /tmp/gomyway-midterm-source.m4a
          ACTUAL_BLOB_SHA="$(git hash-object /tmp/gomyway-midterm-source.m4a)"
          EXPECTED_BLOB_SHA='4dd709e3fa177b4daeed71ca97f0199757729d4b'
          echo "Authorized fixture blob: ${ACTUAL_BLOB_SHA}"
          test "${ACTUAL_BLOB_SHA}" = "${EXPECTED_BLOB_SHA}"

      - name: Install audio decoder
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg

      - name: Decode fixture to mono analysis WAV
        shell: bash
        run: |
          set -euo pipefail
          ffmpeg -hide_banner -loglevel error -y \
            -i /tmp/gomyway-midterm-source.m4a \
            -ac 1 -ar 22050 \
            /tmp/gomyway-midterm-analysis.wav

      - name: Install pinned CPU analysis dependencies
        run: >-
          python -m pip install --disable-pip-version-check
          numpy==2.2.6 scipy==1.15.3 librosa==0.11.0 soundfile==0.13.1

      - name: Rebuild accepted structure evidence reference-blind
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/songsterr-fresh-note-canary
          python scripts/songsterr-fresh/analyze_full_mixture_structure.py \
            --input /tmp/gomyway-midterm-analysis.wav \
            --output /tmp/songsterr-fresh-note-canary/raw-structure-analysis.json \
            --audio-source 'public/jimmy-paige-midterm-v1/gomyway-midterm-source.m4a@main#4dd709e3fa177b4daeed71ca97f0199757729d4b'
          node scripts/songsterr-fresh/build_structure_map.mjs \
            /tmp/songsterr-fresh-note-canary/raw-structure-analysis.json \
            /tmp/songsterr-fresh-note-canary/adapted-structure-map.json
          node scripts/songsterr-fresh/build_note_evidence_context.mjs \
            /tmp/songsterr-fresh-note-canary/adapted-structure-map.json \
            /tmp/songsterr-fresh-note-canary/note-evidence-context.json

      - name: Analyze CPU pitch evidence against frozen structure
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/analyze_structure_conditioned_notes.py \
            --input /tmp/gomyway-midterm-analysis.wav \
            --context /tmp/songsterr-fresh-note-canary/note-evidence-context.json \
            --output /tmp/songsterr-fresh-note-canary/raw-note-evidence-pitch.json \
            --role guitar \
            --audio-source 'public/jimmy-paige-midterm-v1/gomyway-midterm-source.m4a@main#4dd709e3fa177b4daeed71ca97f0199757729d4b' \
            --expected-structure-signature 'fnv1a32:2f493225'
          python - <<'PY'
          import json
          path = '/tmp/songsterr-fresh-note-canary/raw-note-evidence-pitch.json'
          with open(path, 'r', encoding='utf-8') as handle:
              data = json.load(handle)
          if data.get('capabilities', {}).get('durationResolution') != 'none':
              raise SystemExit('PITCH_ANALYZER_MUST_DECLARE_NO_DURATION_AUTHORITY')
          if data.get('diagnostics', {}).get('durationEvidence', {}).get('resolvedCount') != 0:
              raise SystemExit('PITCH_ANALYZER_MUST_RESOLVE_ZERO_DURATIONS')
          leaking = [o.get('onsetId') for o in data.get('onsets', []) if o.get('durationSeconds') is not None or o.get('sourceEnd') is not None]
          if leaking:
              raise SystemExit(f'PITCH_ANALYZER_DURATION_LEAK:{leaking[:5]}')
          PY

      - name: Inspect low-boundary role evidence without reclassification
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/inspect_role_boundary_evidence.py \
            --input /tmp/gomyway-midterm-analysis.wav \
            --evidence /tmp/songsterr-fresh-note-canary/raw-note-evidence-pitch.json \
            --output /tmp/songsterr-fresh-note-canary/raw-note-evidence-pitch-guarded.json

      - name: Estimate conservative selected-pitch release evidence
        shell: bash
        run: |
          set -euo pipefail
          python scripts/songsterr-fresh/estimate_selected_pitch_releases.py \
            --input /tmp/gomyway-midterm-analysis.wav \
            --evidence /tmp/songsterr-fresh-note-canary/raw-note-evidence-pitch-guarded.json \
            --output /tmp/songsterr-fresh-note-canary/raw-note-evidence.json
          python - <<'PY'
          import json
          path = '/tmp/songsterr-fresh-note-canary/raw-note-evidence.json'
          with open(path, 'r', encoding='utf-8') as handle:
              data = json.load(handle)
          release = data.get('diagnostics', {}).get('releaseEvidence', {})
          if release.get('contract') != 'songsterr-fresh-cpu-spectral-release-evidence-v2':
              raise SystemExit('DEDICATED_RELEASE_STAGE_MUST_OWN_DURATION_EVIDENCE')
          if data.get('provenance', {}).get('durationEvidenceSource') != 'songsterr-fresh-cpu-spectral-release-evidence-v2':
              raise SystemExit('DURATION_PROVENANCE_MUST_NAME_DEDICATED_RELEASE_STAGE')
          if release.get('resolvedPromotedOnsetCount', 0) <= 0:
              raise SystemExit('REAL_CANARY_EXPECTS_SOME_EXPLICIT_RELEASE_EVIDENCE')
          PY

      - name: Validate and adapt note evidence
        run: |
          node scripts/songsterr-fresh/build_note_evidence.mjs \
            /tmp/songsterr-fresh-note-canary/note-evidence-context.json \
            /tmp/songsterr-fresh-note-canary/raw-note-evidence.json \
            /tmp/songsterr-fresh-note-canary/adapted-note-evidence.json

      - name: Run pitch-resolved evidence through deterministic pipeline fail-closed
        run: |
          node scripts/songsterr-fresh/run_note_evidence_pipeline_canary.mjs \
            /tmp/songsterr-fresh-note-canary/adapted-structure-map.json \
            /tmp/songsterr-fresh-note-canary/adapted-note-evidence.json \
            /tmp/songsterr-fresh-note-canary/pipeline-canary-result.json
          node --input-type=module - <<'NODE'
          import { readFile } from 'node:fs/promises';
          const result = JSON.parse(await readFile('/tmp/songsterr-fresh-note-canary/pipeline-canary-result.json', 'utf8'));
          const evaluation = result.evidenceEvaluation;
          if (evaluation?.acceptedForCompleteTab !== false) {
            throw new Error('BASELINE_NOTE_EVIDENCE_MUST_NOT_CLAIM_COMPLETE_TAB_ACCEPTANCE');
          }
          for (const expected of [
            'ROLE_RELEVANCE_UNRESOLVED',
            'POLYPHONY_UNRESOLVED',
            'PITCH_EVIDENCE_UNRESOLVED',
          ]) {
            if (!evaluation.failureReasons.includes(expected)) {
              throw new Error(`BASELINE_MUST_EXPOSE_${expected}`);
            }
          }
          if (evaluation.evaluatorContract.compositeScore !== null) {
            throw new Error('NOTE_EVIDENCE_EVALUATOR_MUST_REMAIN_SCORELESS');
          }
          if (result.pipelineSummary.pitchResolvedEventCount <= 0) {
            throw new Error('CANARY_MUST_RETAIN_PITCH_RESOLVED_DIAGNOSTIC_EVENTS');
          }
          if (result.pipelineSummary.roleAcceptedEventCount !== 0) {
            throw new Error('UNRESOLVED_ROLE_MUST_EXPOSE_ZERO_ROLE_ACCEPTED_EVENTS');
          }
          if (result.pipelineSummary.completeTabEligibleEventCount !== 0) {
            throw new Error('FAILED_EVIDENCE_MUST_EXPOSE_ZERO_CUSTOMER_ELIGIBLE_EVENTS');
          }
          if (result.pipelineSummary.upstreamEvidenceReady !== false) {
            throw new Error('PRODUCT_SHELL_MUST_RECEIVE_FAILED_UPSTREAM_EVIDENCE_GATE');
          }
          if (result.pipelineSummary.deliveryReady !== false) {
            throw new Error('INCOMPLETE_REAL_NOTE_EVIDENCE_MUST_FAIL_CLOSED_FROM_DELIVERY');
          }
          NODE

      - name: Re-run fresh deterministic suite
        working-directory: songsterr_pipeline
        run: npm test

      - name: Show compact note-evidence diagnostics
        shell: bash
        run: |
          python - <<'PY'
          import json
          for name in ('raw-note-evidence-pitch.json', 'raw-note-evidence-pitch-guarded.json', 'raw-note-evidence.json', 'adapted-note-evidence.json', 'pipeline-canary-result.json'):
              path = f'/tmp/songsterr-fresh-note-canary/{name}'
              with open(path, 'r', encoding='utf-8') as handle:
                  data = json.load(handle)
              print(f'--- {name} ---')
              print(json.dumps({
                  'structureIdentity': data.get('structureIdentity'),
                  'capabilities': data.get('capabilities'),
                  'diagnostics': data.get('diagnostics'),
                  'metrics': data.get('metrics'),
                  'adapterContract': data.get('adapterContract'),
                  'eventExposure': data.get('eventExposure'),
                  'evidenceEvaluation': data.get('evidenceEvaluation'),
                  'pipelineSummary': data.get('pipelineSummary'),
                  'provenance': data.get('provenance'),
              }, indent=2))
          PY

      - name: Upload clean note-evidence artifacts
        uses: actions/upload-artifact@v4
        with:
          name: songsterr-fresh-gomyway-midterm-note-evidence
          path: /tmp/songsterr-fresh-note-canary/*.json
          if-no-files-found: error
          retention-days: 14
