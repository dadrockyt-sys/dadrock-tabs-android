name: Compact Tab Studio first page

on:
  push:
    paths:
      - '.github/workflows/compact-tab-studio-first-page.yml'

permissions:
  contents: write

jobs:
  patch-tab-studio:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Keep logo and maximize first-page tab space
        run: python .github/scripts/compact_tab_studio_first_page.py

      - name: Commit tighter first-page layout
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/admin/tab-studio/page.js
          git diff --cached --quiet || git commit -m "Remove redundant Tab Studio subtitle from PDF"
          git push
