name: V168 shared-context result checkpoint

on:
  push:
    branches:
      - v143-contextual-prune-lobo
    paths:
      - .github/workflows/v168-shared-context-result-checkpoint.yml

permissions:
  contents: write

jobs:
  checkpoint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v143-contextual-prune-lobo
          fetch-depth: 0
      - name: Append checkpoint
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          p = Path('docs/checkpoints/CURRENT_STATE.md')
          marker = '## 2026-09-01 continuation checkpoint — shared-context functional-equivalence result'
          if marker in p.read_text():
              raise SystemExit('checkpoint marker already present')
          append = r'''

## 2026-09-01 continuation checkpoint — shared-context functional-equivalence result
- Before execution, added isolated reference-blind checker `validation/v168_splitmysong_diagnostic/check_shared_context_equivalence_v168.py`, creation commit **`73af04264c13e5daddb5b7fc1db4e0bd9dcea149`**, frozen Git blob **`2e9a8dc2ec60a311b3e6bca5f8dc9390aea48b0f`**. Its acceptance rule was fixed in code before the diagnostic result was observed.
- Exact V166 Actions run `33226705813` has no surviving uploaded artifacts, and historical `.github/workflows/v166-generate.yml` at head `1e06d775ee03ebf92fe8f68fc02cf034812ea43f` contains no `upload-artifact` or persistent stem-cache step. The authoritative historical Demucs Drums WAV bytes therefore cannot currently be recovered from that run.
- Frozen V166 identities used by the checker: candidate SHA256 **`fa2411598b401f745eff49a9cbda294ed767de093c905909531c7dd4dc6eb378`**, timebase SHA256 **`899746d3048d239bc0032375d412a109ea04b055df19df1b7b08dc3e73aa5ca0`**, normalized mix SHA256 **`3e61b7926eabc21b758c750f826c7426a29d6de5aafdd5c93f8045ecdc67f87e`**, 1050 combined-Guitar events, 402 Bass events, 1805 lattice steps.
- Frozen checker acceptance: PASS requires **all 1805 lattice steps covered by historical persisted option rows**, zero inconsistent repeated historical shared-support values, zero stored historical score recomputation mismatches, zero frozen winner recomputation mismatches, and zero current selected-step changes. Incomplete coverage must return `FAIL_CLOSED` even if all observed event decisions agree. The checker does not claim WAV-byte equivalence.
- GitHub Actions diagnostic workflow `.github/workflows/v168-shared-context-equivalence.yml` run **`33564860461`**, job **`100045591125`**, head **`58342c8c5cc63b945fb4d56c658f47015d0a9ce6`**, completed successfully as an execution workflow; the scientific checker itself returned **`OBSERVED_EVENT_NEIGHBORHOODS_EQUIVALENT_BUT_LATTICE_COVERAGE_INCOMPLETE / FAIL_CLOSED`** with exit code 2 as preregistered.
- Fresh drifted Drums SHA256 in that run: **`450fa2deada5b4222039473eb2bec4f012b9e09b754c7d70d75a0f12430f8ec2`**; frozen authoritative V166 Drums SHA256 remains **`05890ac9cad62eacf0099c962b137a458228811a85b8ea828bb15f238d2c1e50`**.
- Across all **1452** frozen V166 events and **4356** persisted option rows, current selected-step changes were **0**; frozen winner recomputation mismatches **0**; stored historical score recomputation mismatches **0**; inconsistent repeated historical-support steps **0**; changed-decision examples empty.
- Historical option rows covered **1617/1805 lattice steps = 89.58448753462604%**. **188 lattice steps remain uncovered**, so the strict gate does not authorize use of the drifted Drums context for one-shot SplitMySong generation.
- Continuous drift on covered evidence was very small but nonzero: maximum absolute shared-support delta **`0.0001594903414565696`** and maximum absolute event-step-score delta **`1.5949034145645857e-05`**. Private report SHA256 printed by the run: **`3c49f2a87cdf1ca47161ed19a4eb47406cc542b8310f70f508073d7ee9ee8f87`**; no report/audio/stem artifact was uploaded.
- The workflow used no private SplitMySong audio. Basic Pitch was installed only for historical dependency parity but the checker reports `basicPitchImported=false` and `pitchInferenceInvoked=false`; no candidate was generated, no scorer/reference was read, and reference-facing score calls remain 0.
- **Next safe step:** determine whether the 188 uncovered lattice steps can be filled from other already-frozen reference-blind artifacts that provably share the exact V166 timebase and historical shared-onset context. Do not weaken the full-coverage rule and do not run the one-shot SplitMySong candidate generator unless a new pre-registered gate reaches PASS.
- **Project Progress Score remains 60%; Test Score remains NOT RUN; SplitMySong Basic Pitch attempts remain 0; V168 reference-facing score calls remain 0.** No GPU/CUDA/Modal, `main`, or Production change occurred.
'''
          p.write_text(p.read_text() + append)
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/checkpoints/CURRENT_STATE.md
          git commit -m "Checkpoint shared-context functional-equivalence result"
          git push origin HEAD:v143-contextual-prune-lobo

# Deliberate second-commit trigger after workflow registration.
