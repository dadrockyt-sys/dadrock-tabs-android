name: V144 Rhythm Fit-Only Error Mechanism Diagnostic — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: Refuse replay of completed fit error mechanism diagnostic
    runs-on: ubuntu-latest
    steps:
      - name: Refuse replay
        shell: bash
        run: |
          echo 'The V144 fit-only error-mechanism diagnostic is complete and archived. Use its persisted report; do not replay it.' >&2
          exit 1
