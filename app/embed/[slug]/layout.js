export const metadata = {
  title: 'DadRock Tabs Embedded Lesson',
  robots: {
    index: false,
    follow: true,
    googleBot: {
      index: false,
      follow: true,
    },
  },
};

export default function EmbedLayout({ children }) {
  return children;
}
