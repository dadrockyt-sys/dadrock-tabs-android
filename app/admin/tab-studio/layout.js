export const metadata = {
  title: 'JPG Tab Studio | DadRock Tabs Admin',
  description: 'Private DadRock admin tool for assembling original JPG tab pages into a professional PDF.',
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminTabStudioLayout({ children }) {
  return children;
}
